namespace Controllers
{ /// <summary>
/// The ICommand interface as  Execute method  which would execute any command that a button tells it too.
/// </summary>
    interface ICommand
    {
        void Execute();
    }
}
